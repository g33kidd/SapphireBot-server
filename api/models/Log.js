/**
 * Specifically this is for keeping track of what's been
 * done through the dashboard.
 *
 * Each entry should be accompanied by an action and changes.
 */

module.exports = {
	connection: 'local_mongo',
	attribute: {
		action: {
			type: 'string'
		}
	}
}